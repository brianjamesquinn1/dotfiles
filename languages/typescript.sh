# Install Typescript
# Note that LSPs will often not attempt to start a js/ts
# server unless node modules are present, so make sure to
# run 'npm i' on new projects beforehand

npm install -g typescript-language-server typescript
